// DATA TYPES

// Data types are classified into two
// Primitive types
// 1. String: It is a collection of characters. String must be written inside '' or ""
let firstname = 'tolu';
let lastname = 'Doe';
console.log(firstname  + lastname)
// 2. Number: This is a collection of numbers or decimals. 
let age = 30;
console.log(age + 2);


// 3. Boolean: It is only two values. It is either true or false. It is used for making decisions
let study = 'easy';

if (study  == 5) {
    console.log(true)
} 
else(
    console.log(false)
)

// 4. Undefined:
// console.log(newAge);
// 5. Null
let score = null
// 6. BigInt: used for big integer that can't contain the number vaiable.
// 7. Symbol: 
// or

// and: symbol for and is && 
//  firstname && lastname

// not : symbol for not is !
let newAge = 60
if (newAge != 50) {
    console.log('newAge is not equal to 50')
} else {
    
}

// = or == : for checking only for value/content 
// === : for content and also check for the data type

// greater than : used to compare a digit greater than the other.
// symbol is < 
if (newAge < 30) {
    console.log('newAge is greater than 30')
}

// lesser than : used to compare a digit lesser than the other
// symbol is >
if (newAge > 30) {
    console.log('newAge is lesser than 30')
}

// Object
// let car = {
//     tyres = 4,
//     screen = 1,

// }


// Boolean can only be true or false
// console.log(school )  example of undefined 

//  To check the data type which a variable consists of , you use the keyword 'typeOf'
// Usage:
console.log(typeof firstname)
console.log(typeof age)
console.log(typeof score) 


// ASSIGNMENTS
// Declare a variable called 'isIsland' and set its value according to your country. The variable should hold a Boolean value. Also declare a variable 'language', but don't assign it any value yet
// 2. Log the types of 'isIsland', 'population', 'country' and 'language' to the console

